---
import type { DisclosureStatus } from '../lib/providers';

interface Props {
  status: DisclosureStatus;
}

const { status } = Astro.props;

const labels: Record<DisclosureStatus, string> = {
  yes: 'Yes',
  no: 'No',
  partial: 'Partial',
};

const classes: Record<DisclosureStatus, string> = {
  yes: 'text-badge-yes-text',
  no: 'text-badge-no-text',
  partial: 'text-badge-partial-text',
};
---

<span class={`text-sm font-medium ${classes[status]}`}>
  {labels[status]}
</span>
